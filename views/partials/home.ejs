
<main>
  <!-- The inline SVG element -->
  <!-- viewBox="0 0 100 100" sets up a coordinate system from 0 to 100 -->
  <!-- width/height 100% and preserveAspectRatio="none" ensure it covers the viewport -->
  <svg class="reveal-svg" viewBox="0 0 100 100" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg">
    <defs>
      <!-- Define the mask -->
      <mask id="paintSweepMask">
        <!--
          Start with a WHITE background rectangle in the mask.
          In SVG masks, white areas make the masked element VISIBLE.
          This initially keeps the 'coverRect' visible, hiding content below.
        -->
        <rect x="0" y="0" width="100" height="100" fill="white"/>

        <!--
          The path that defines the drawing shape - a zigzag pattern moving downwards.
          Using cubic Bezier curves (C command) for smoother transitions.
        -->
        <!--
          This path has a BLACK stroke. Black areas in the mask make the masked element TRANSPARENT (hidden).
          As this black stroke is drawn over the white mask background, it creates black areas,
          which reveal the underlying HTML content.
        -->
        <path id="paintStrokePath"
                /*
                Path definition:
                M 0 0  - Move to starting point (top left)
                C 25 0 75 10 100 10 - Curve right and slightly down
                C 75 10 25 20 0 20   - Curve left and slightly down
                C 25 20 75 30 100 30 - Curve right and slightly down
                C 75 30 25 40 0 40   - Curve left and slightly down
                ... (repeat pattern, incrementing Y by 10 for each pair of segments)
                C 25 80 75 90 100 90 - Curve right and slightly down
                C 75 90 25 100 0 100 - Curve left and slightly down, ending at the bottom left
              */
              d="M 0 0
                C 25 0 75 10 100 10
                C 75 10 25 20 0 20
                C 25 20 75 30 100 30
                C 75 30 25 40 0 40
                C 25 40 75 50 100 50
                C 75 50 25 60 0 60
                C 25 60 75 70 100 70
                C 75 70 25 80 0 80
                C 25 80 75 90 100 90
                C 75 90 25 100 0 100
                "
              fill="none" /* The path itself has no fill */
              stroke="black" /* The stroke color IN THE MASK (black hides, revealing content) */
              stroke-width="20" /* Make the stroke thick enough to cover the vertical gaps (10 units between strokes) */
              stroke-linecap="round"/> /* Makes the ends rounded for smoother coverage */

          <!-- The CSS animation handles the drawing, so no <animate> tag needed here -->

      </mask>
    </defs>

    <!-- The element that initially covers the screen. -->
    <!-- It's a rectangle covering the whole SVG (which covers the viewport). -->
    <!-- It has a solid fill (your "paint" color, e.g., white or black). -->
    <!-- This rect is applied the mask defined above. -->
    <rect x="0" y="0" width="100" height="100" fill="black" mask="url(#paintSweepMask)"/>
    <!-- You could change fill="white" to fill="black" if you want a black starting screen -->

  </svg>          
  
  <div class="">

    <div class="slideshow-container">
      <!-- Slide 1 -->
      <div class="slide active">Slide 1 Content</div>

      <!-- Slide 2 -->
      <div class="slide">Slide 2 Content</div>

      <!-- Slide 3 -->
      <div class="slide">Slide 3 Content</div>

      <!-- Slide 4 -->
      <div class="slide">Slide 4 Content</div>

      <!-- Slide 5 -->
      <div class="slide">Slide 5 Content</div>
  </div>

  </div>
  
  

</main>
